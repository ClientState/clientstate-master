// Generated by CoffeeScript 1.8.0
(function() {
  var Model, knex, name, s, _ref;

  knex = require("./conn").knex;

  _ref = require("./models");
  for (name in _ref) {
    Model = _ref[name];
    console.log("create table for", name);
    s = knex.schema.createTableIfNotExists(Model.tableName, Model.createTable).then(function(a, b, c) {});
    s["yield"]();
  }

  setTimeout(process.exit, 100);

}).call(this);
